To install and run this cheesy VPN do this:

1. Run OpenVPN-2.6.14-I001-amd64.msi (if on Windows)
- extract and make the openvpn-2.6.14.tar.gz (if on Linux)
- also you can manually install OpenVPN if wanted

2. Make sure to install python for your whole pc!
- type python in cmd on Windows for example

3. Install essential libs via running libs_install (TO INSTALL LIBS).bat for example

4. Run auto_openvpn (RUN ME).bat

5. Enjoy!!!